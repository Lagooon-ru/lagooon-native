import { createEffect } from 'effector';

const signupEffect = createEffect();

const loginEffect = createEffect();

const logoutEffect = createEffect();

const forgetEffect = createEffect();

const resetEffect = createEffect();

export { signupEffect, loginEffect, logoutEffect, forgetEffect, resetEffect };
